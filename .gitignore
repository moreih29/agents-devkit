node_modules/
.DS_Store
*.log

# runtime state (nx MCP가 생성)
.nexus/state/
.nexus/runtime/

# dist/는 commit — marketplace install 시 node_modules 없음

# codex-nexus 플러그인 산출물 — 추적 안 함
.codex/
.agents/
plugins/
